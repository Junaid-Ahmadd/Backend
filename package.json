{
  "name": "screenshot-service",
  "version": "1.0.0",
  "description": "Screenshot service using Playwright",
  "main": "dist/function.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "predeploy": "npm run build",
    "deploy": "func azure functionapp publish last-backend"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@types/node": "^20.10.4",
    "playwright-chromium": "^1.40.0",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.0.5455",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
